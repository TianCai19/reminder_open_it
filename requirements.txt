# GUI 依赖 (Python 内置)
# tkinter 是 Python 内置模块

# 音效播放
pygame>=2.5.0

# 图像处理和界面美化
Pillow>=9.0.0

# 现代化主题 (可选)
ttkthemes>=3.2.2

# Web 方案
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
openai>=1.50.2

# 打包工具
pyinstaller>=5.10.1

# 其他可能需要的依赖
# 如果后续添加了其他第三方库，请在这里添加
